<link href="/plugins/apex/apexcharts.css" rel="stylesheet" type="text/css">
<link href="/assets/css/dashboard/dash_2.css" rel="stylesheet" type="text/css" />
<link href="/assets/css/elements/custom-pagination.css" rel="stylesheet" type="text/css" />

<div class="row px-4">
    <div class="col-10">.</div>
    <div class="col-2">
        <a href="/users" class="bg-primary px-3 py-1 rounded">
            <i class="bi bi-box-arrow-left"></i>
            volver
        </a>
    </div>

    <div class="mt-3 col-12">
        {{#if users}}

            <div class="table-responsive">
                <table class="table table-hover table-light table-bordered mb-4">
                    <thead>
                        <tr>
                            <th>Usuario</th>
                            <th>Correo</th>
                            <th>Creado</th>
                            <th></th>
                        </tr> 
                    </thead>
                    <tbody>
                        {{#each users}}
                        <tr>
                                <td>{{this.username}}</td>
                                <td>{{this.email}}</td>
                                <td>
                                    {{#formatDate this.create_at}}
                                        {{this}}
                                    {{/formatDate}}
                                </td>
                                <td>
                                    <a href="/users/{{this._id}}"> <i class="bi bi-eye-fill"></i> </a>
                                </td>
                        </tr> 
                        {{/each}}
                    </tbody>
                </table>
            </div>

            <div class="row">
                <div class="col-6">
                    <span class="h6">{{nowTotal}}</span>
                </div>

                <div class="paginating-container pagination-default col-5">
                    <ul class="pagination">
                        {{#if nowPathOne}}
                            <li class="prev"><a href="{{previousPath}}">anterior</a></li>
                        {{/if}}
                            <li class=""><span class="px-5">pagina actual {{nowPath}}</span></li>
                        {{#if nowPathEnd}}
                            <li class="next"><a href="{{nextPath}}">siguiente</a></li>
                        {{/if}}
                    </ul>
                </div>
            </div>
            

        {{else}}
        {{/if}}
    </div>
</div>
